{
    "name": "@autonoma/db",
    "version": "0.0.2",
    "private": true,
    "type": "module",
    "exports": {
        ".": "./src/index.ts",
        "./env": "./src/env.ts"
    },
    "scripts": {
        "build": "tsc --noCheck",
        "typecheck": "tsc --noEmit",
        "lint": "oxlint --fix src/",
        "fmt": "oxfmt --write src/",
        "test": "vitest run --passWithNoTests",
        "db": "dotenv -e ../../.env -- prisma",
        "db:generate": "DATABASE_URL=${DATABASE_URL:-postgres://postgres:postgres@localhost:5432/postgres} pnpm run db -- generate",
        "postinstall": "pnpm run db:generate",
        "db:migrate": "pnpm run db -- migrate dev",
        "db:seed": "dotenv -e ../../.env -- tsx src/seed.ts"
    },
    "dependencies": {
        "@autonoma/types": "workspace:*",
        "@autonoma/utils": "workspace:*",
        "@prisma/adapter-pg": "catalog:",
        "@prisma/client": "catalog:",
        "@t3-oss/env-core": "catalog:",
        "zod": "catalog:"
    },
    "devDependencies": {
        "@types/node": "catalog:",
        "ai": "catalog:",
        "better-auth": "catalog:",
        "dotenv": "catalog:",
        "dotenv-cli": "^11.0.0",
        "prisma": "catalog:",
        "prisma-json-types-generator": "^5.1.1",
        "tsx": "catalog:",
        "typescript": "catalog:",
        "vitest": "catalog:"
    }
}
